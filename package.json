{
  "name": "site-1",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "deploy": "astro build && gh-pages -d dist"
  },
  "dependencies": {
    "astro": "^4.16.0"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0"
  }
}
